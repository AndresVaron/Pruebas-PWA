{
  "name": "taller1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "build": "npm run clean && npm run copy && workbox generateSW"
  },
  "dependencies": {
    "express": "~4.16.1",
    "workbox-cli": "^5.1.3"
  }
}
